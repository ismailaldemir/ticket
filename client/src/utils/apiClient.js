/**
 * API İstek Yardımcı Modülü
 *
 * Bu modül, mevcut api.js'i kullanarak uygulamadaki tutarlılığı sağlar.
 * E-posta modülü ve diğer modüllerde apiClient olarak import edilebilir.
 */

import apiClient from "./api";

export default apiClient;
